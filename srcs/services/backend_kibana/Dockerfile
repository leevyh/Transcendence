FROM kibana:8.14.1

ENV SERVERNAME=kibana
ENV ELASTICSEARCH_HOSTS=https://backend_elastic:9200
ENV ELASTICSEARCH_USERNAME=kibana_system
ENV ELASTICSEARCH_PASSWORD=${KIBANA_PASSWORD}
ENV ELASTICSEARCH_SSL_CERTIFICATEAUTHORITIES=config/certs/ca/ca.crt